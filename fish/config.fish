# Aliases
alias g++ "g++ -std=c++11 -Wall -Wextra -g"
alias clang++ "clang++ -std=c++11 -Wall -Wextra -g"

# Vim keybindings
fish_vi_key_bindings

# Abbreviations
abbr -a pacman sudo pacman -S
abbr -a shutdown sudo shutdown now
abbr -a reboot sudo reboot
abbr -a size du -h --max-depth=1
